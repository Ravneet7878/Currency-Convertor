# Currency-Convertor

# **API Link**

To get the API link for the currency, use the following template:

```javascript
let api_link = `https://cdn.jsdelivr.net/npm/@fawazahmed0/currency-api@latest/v1/currencies/${currency}.json`
